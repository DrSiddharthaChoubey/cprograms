#include<stdio.h>
#define SIZE1 5
#define SIZE2 5
#define SIZE3 10
int main()
{
int a[SIZE1],a[SIZE2],a[SIZE3],i,j;
printf("enter the elements of array a:");

for(i=0;i<SIZE1;i++)
 scanf("%d",&a[i]);
i=0,j=0,k=0
while(i<SIZE1,j=n-1;i<=j;i++,j--)
  {
    temp=a[i];
     a[i]=a[j];
      a[j]=temp;
   }
printf("after reverse array elements are-->");
for(i=0;i<n;i++)
printf("%d",a[i]);
return 0;
}
