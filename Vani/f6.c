#include<stdio.h>
int main()
{
           int n,p;
           printf("enter the number");
           scanf("%d",&n);
           p=isprime(n);
           if (p==1)
           printf("number is prime");
           else
           printf("number is not prime");
           

return 0;
}

     int is prime(intx)
     int i=2
     while(i<=x)
   
{
     if(x%==0)
      break;
 else 
     i++;
    
}

  if(i==x)
  return 1;

 else 
return 0;

}    
